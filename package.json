{
  "name": "forkify",
  "version": "1.0.0",
  "description": "this is the final project of jonas react course which i would be building by typing code out myself instead of copying from the tutorial ",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html"
  },
  "author": "EngrPips",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.10.0",
    "parcel": "^2.10.0"
  },
  "dependencies": {
    "core-js": "^3.33.1",
    "regenerator-runtime": "^0.14.0"
  }
}
